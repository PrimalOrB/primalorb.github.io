import MainContent from "../Main"
import Subtitle from "../Subtitle"

const Resume = () => {
    const title = 'resume'
    return (
        <MainContent>
            <Subtitle title={ title }/>
        </MainContent>
    )
}

export default Resume